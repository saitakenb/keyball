name: Build takeshi firmware

on:
  push:
    branches:
      - '*'
  workflow_dispatch: {}

jobs:
  build:
    uses: ./.github/workflows/build-firmware.yml
    with:
      qmk_version: '0.22.14'
      keyboard: 'keyball61'
      keymap: 'takeshi'
