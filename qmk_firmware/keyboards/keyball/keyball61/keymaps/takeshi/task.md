# タスク: スレーブ側物理直接出力による同期と全点灯上書きの完全解決

- [x] 元のバージョンを `keymap_original.c` として退避・同期
- [x] rules.mk / config.h の Caps Word 無効化によるサイズ大幅削減の適用
- [x] `keymap.c` (scratch側) の `get_is_left_hand_latched` を知見集バグ③対策（スキャンカウンタ＆ボールセンサー検出）によるラッチ特定ロジックに変更し、スレーブ・マスターに関わらず起動時の左右判定エラーを完全解消
- [x] QMK標準の物理LED送信関数 `rgblight_call_driver` (weak属性) を `keymap.c` 側で完全にオーバーライド
- [x] 送信直前に渡されるLEDバッファ `start_led` に対して、マスター/スレーブ共通で、消灯クリアおよび `sat` (点灯数) に基づく部分点灯カラー上書きを実行するよう修正
- [x] スレーブ側の `matrix_scan_user` での無駄な描画や物理送信などのハック処理を完全に廃止し、割り込みによる波形破壊を100%防止
- [x] 修正した `keymap.c` をコンパイルディレクトリ `/mnt/c/Users/saita/qmk_firmware` へコピー
- [x] キャッシュクリーンした上でビルドを実行し、サイズ制限内に余裕で収まることを確認 (残り294バイト)
- [x] 変更をローカルにコミットし、GitHub (`myfork/main`) へプッシュ完了
